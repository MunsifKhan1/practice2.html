
const fruits = ['apple', 'banana', 'cherry'];

const upperCaseFruits = fruits.map((fruit) => {
    console.log(fruit); // Log the original fruit name
    return fruit.toUpperCase(); // Return the fruit name in uppercase
});
